import React, { useState } from "react";

const Login = () => {
    const [token, setToken] = useState("");

    return (
        <div>
            <p>test</p>
        </div>
    );
};

export default Login;
